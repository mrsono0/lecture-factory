# 마이크로 세션: 089-데이터를 담는 특별한 그릇, @dataclass

## 📋 메타 정보
| 항목 | 값 |
|------|-----|
| **세션 ID** | MS-AI-native_파이썬기초-089 |
| **소요 시간** | 20분 |
| **예상 교안 분량** | 3,000자 |
| **선행 세션** | 세션-088 (필수) |
| **후행 세션** | 세션-090 |
| **핵심 키워드** | `@dataclass`, 데이터 저장, 데코레이터 |
| **난이도** | medium |
| **청크 타입** | code |

## 🎯 학습 목표
단 1개의 구체적인 학습 목표 (SMART 원칙 적용):
- 이 세션 종료 시 수강생은 일반 클래스의 `__init__` 방식과 `@dataclass`를 비교하여, `@dataclass`가 데이터 중심 클래스 작성에 주는 이점을 설명할 수 있다.

## 🗣️ 강사 대본 구성

### 예상 분량
- **최소**: 공백 포함 1,500자
- **최대**: 공백 포함 2,000자
- **화법**: 친근한 구어체 (~해요, ~입니다)

### 서술 흐름
1. **도입 (Hook)**: 방금 전 실습에서 `__init__`과 `self.name = name`을 반복적으로 쳤던 귀찮음을 짚어내며 공감을 유도합니다.
2. **비유 (Analogy)**: '반자동 포장 기계' 비유. 매번 상자를 접고(`__init__`) 라벨을 붙이는(`self`) 수작업 대신, 기계에 "이름, 전화번호, 이메일 칸 만들어!"라고 스펙만 주면 알아서 상자를 완성해 주는 반자동 기계를 소개합니다.
3. **개념 설명 (What)**: 파이썬 3.7부터 도입된 `@dataclass`의 개념을 소개합니다. 이름에 붙은 `@` 모양 기호를 데코레이터(Decorator)라고 부르며, 클래스를 포장지로 예쁘게 감싸서 마법 같은 기능을 부여한다고 설명합니다.
4. **실제 활용 (How)**: 기존 Customer 클래스와 `@dataclass`가 적용된 Customer 클래스 코드를 나란히 띄워놓고 비교합니다. `__init__`이 사라지고 변수 이름과 타입(Type Hint)만 나열된 깔끔한 형태를 보여줍니다.
5. **정리 (Checkpoint)**: 데이터를 주로 담는 그릇 역할의 클래스를 만들 때는 `@dataclass`를 쓰면 코드가 훨씬 짧고 명확해진다는 점을 강조하고 세션을 마무리합니다.

### 예시 스크립트
> "여러분, 앞서 `__init__` 함수 만들 때 `self.name = name`, `self.phone = phone`... 계속 반복해서 치니까 좀 지루하지 않으셨나요? 파이썬을 만든 귀도 반 로섬 할아버지도 똑같이 생각했나 봐요. '아, 그냥 데이터만 담을 건데 타이핑하기 너무 귀찮다!' 해서 파이썬 3.7 버전에 도입된 것이 바로 `@dataclass`입니다.
> 클래스 바로 위에 모자처럼 `@dataclass`를 씌워주면, 파이썬이 뒤에서 몰래 `__init__` 함수를 알아서 뚝딱 만들어줍니다. 마치 수작업 포장을 하다가, 버튼 하나 누르면 박스가 접히는 반자동 기계를 들여놓은 것과 같아요. 코드가 절반으로 줄어드는 마법을 지금부터 눈으로 확인해 보겠습니다."

## 🎙️ 실습 가이드 (해당 시)

### 실습 목표
- 이 실습을 통해 수강생은 AI 프롬프트를 이용해 기존 클래스를 `@dataclass`로 변환해 보고 그 간결함을 확인한다.

### 단계별 지시
| 단계 | 소요 시간 | 강사 지시사항 | 예상 결과 |
|------|----------|--------------|----------|
| 1 | 3분 | "AI에게 '앞서 만든 Customer 클래스를 `@dataclass`를 사용하는 방식으로 바꿔줘'라고 요청해 보세요." | `@dataclass`가 적용된 간결한 코드 출력 |
| 2 | 5분 | "코드를 복사해서 Antigravity IDE에서 실행해보고, 기존과 똑같이 인스턴스를 만들 수 있는지 확인하세요." | `__init__`을 명시하지 않아도 정상적으로 인스턴스가 생성됨 |
| 3 | 2분 | "두 버전의 코드를 나란히 두고, 무엇이 사라졌는지 찾아보세요." | `__init__`과 `self` 할당 코드가 생략되었음을 깨달음 |

### 주의사항 및 트러블슈팅
- 흔한 오류 1: `from dataclasses import dataclass` 임포트 누락 (해결법: 파이썬에서 제공하는 추가 기능이므로 맨 위에 임포트 코드가 반드시 필요함을 짚어줍니다.)
- 흔한 오류 2: 타입 힌트(`: str` 등) 누락 (해결법: `@dataclass`는 타입 힌트를 기준으로 동작하기 때문에 반드시 타입을 적어야 한다고 설명합니다.)

## 💡 비유 및 스토리텔링

### 핵심 비유
- **비유**: 수작업 포장 vs 반자동 포장 기계(`@dataclass`)
- **설명**: 수작업 포장은 `__init__`을 열고 `self.name = name`을 하나하나 적어주는 과정입니다. 반자동 기계는 "이름: 문자열"이라고 스펙만 명시하면 파이썬이 알아서 내부적으로 수작업 코드를 생성해 줍니다.
- **AI 시대의 서사 연결**: AI가 우리의 반복 작업을 줄여주듯이, 파이썬 언어 자체도 개발자의 단순 반복 타이핑(Boilerplate code)을 줄이는 방향으로 진화해왔음을 보여줍니다.

### 스토리텔링 포인트
- 감정적 연결: "코드가 짧아진다 = 내가 칠 오타가 줄어든다 = 퇴근이 빨라진다"는 실용적인 장점을 어필합니다.
- 전환 멘트: "자, 이제 데이터도 완벽하게 담을 수 있고 행동도 지시할 수 있게 되었어요. 이 강력한 '객체지향(OOP)' 무기를 들고, 어제 만든 절차적 고객관리 프로그램을 대공사(리팩토링) 하러 가보겠습니다!"

## 🔗 의존성 및 연결성

### 필수 선행 지식
- 세션-088: 나만의 클래스 만들기 실습 (기존 클래스 작성의 귀찮음을 체감한 상태)

### 권장 선행 지식
- 세션-041: 파이썬의 여러 가지 자료형 (타입 힌트 이해를 위해)

### 후속 연결
- 세션-090: 객체지향 리팩토링과 캡슐화 (부서 조직 재편)

### 브릿지 노트 예시
> "이렇게 클래스의 기초 문법과 최신 트렌드인 `@dataclass`까지 모두 익히셨습니다! 무기를 잘 갈고 닦았으니, 이제 실전에 투입해야죠? 다음 세션(Day 5-2)에서는 어제 만든 '절차적' 고객관리 프로그램을 '객체지향'으로 완전히 뒤엎는 리팩토링을 시작해 보겠습니다."

## 📝 Gemini 프롬프트 최적화 태그
```yaml
gemini_optimized:
  chunk_type: code
  complexity: medium
  estimated_chars: 3000
  output_style: continuous_prose
  tone: friendly_spoken_korean
  include:
    - instructor_script: true
    - lab_guide: true
    - analogy: detailed_situation
    - mermaid_diagram: false
  prerequisites:
    mandatory: [88]
    recommended: [41]
```

## ✅ 체크리스트 (작성자 자가 검증)
- [x] 단 1개의 핵심 학습 목표만 명시했는가?
- [x] 소요 시간이 15~25분 범위인가?
- [x] 강사 대본 예상 분량이 1,500~2,000자 범위인가?
- [x] chunk_type, complexity 태그를 부여했는가?
- [x] 선행/후행 세션을 명확히 연결했는가?
- [x] 비유가 'AI 시대의 서사' 톤과 일치하는가?
