# 마이크로 세션: 099-DB 저장 모듈(SQLiteStorage) 구현

## 📋 메타 정보
| 항목 | 값 |
|------|-----|
| **세션 ID** | MS-AI-native_파이썬기초-099 |
| **소요 시간** | 25분 |
| **예상 교안 분량** | 4,000자 |
| **선행 세션** | 세션-098 (필수) |
| **후행 세션** | 세션-100 |
| **핵심 키워드** | 데이터베이스, SQLite, 저장소 모듈, `SQLiteStorage` |
| **난이도** | medium |
| **청크 타입** | code |

## 🎯 학습 목표
단 1개의 구체적인 학습 목표 (SMART 원칙 적용):
- 이 세션 종료 시 수강생은 파이썬 내장 `sqlite3` 모듈을 활용하여, `FileStorage`와 동일한 메서드(`save`, `load`)를 가지는 `SQLiteStorage` 데이터베이스 저장 모듈을 구현할 수 있다.

## 🗣️ 강사 대본 구성

### 예상 분량
- **최소**: 공백 포함 1,500자
- **최대**: 공백 포함 2,000자
- **화법**: 친근한 구어체 (~해요, ~입니다)

### 서술 흐름
1. **도입 (Hook)**: JSON 파일 저장은 초보자에게 좋지만, 데이터가 수만 건이 되면 파일을 매번 통째로 읽고 쓰는 것이 너무 느려진다는 실무의 한계를 짚어줍니다.
2. **비유 (Analogy)**: '종이 장부' vs '전자 엑셀 시스템'. 파일 저장은 매번 종이 장부를 처음부터 끝까지 넘겨봐야 하지만, DB(데이터베이스)는 엑셀처럼 표로 딱딱 정리되어 있어서 특정 고객을 1초 만에 찾아낼 수 있는 전문 시스템이라고 비유합니다.
3. **개념 설명 (What)**: 파이썬에는 기본적으로 `sqlite3`라는 가벼운 데이터베이스가 내장되어 있음을 소개합니다. 복잡한 SQL 문법을 수강생이 다 알 필요는 없으며, 이것도 AI에게 뚝딱 만들어달라고 하면 됨을 안심시킵니다.
4. **실제 활용 (How)**: 여기서 가장 중요한 원칙을 설명합니다. "기존에 만든 `FileStorage`와 똑같이 `save()`와 `load()`라는 이름을 가진 메서드를 써야 합니다!" 그래야 자동차(CustomerManager)가 타이어를 교체할 때 나사 구멍이 딱 맞게(인터페이스 통일) 들어가기 때문입니다.
5. **정리 (Checkpoint)**: 이제 우리는 '일반 타이어(파일)'와 '스노우 타이어(DB)' 두 세트를 모두 보유하게 되었음을 축하하며, 다음 세션에서 자동차에 끼워보는 최종 테스트를 예고합니다.

### 예시 스크립트
> "고객이 10명일 때는 JSON 파일로 충분해요. 하지만 100만 명이라면 어떨까요? 파일 끝에 한 명 추가하려고 100만 명의 데이터를 전부 다 다시 쓰는 건 너무 비효율적입니다. 실무에서는 무조건 데이터베이스(DB)라는 전용 창고를 씁니다.
> 파이썬에는 `sqlite3`라는 아주 가볍고 빠른 DB가 공짜로 들어있어요. 자, 두 번째 타이어 공장을 세울 차례입니다. `sqlite_storage.py` 파일을 만들어보세요. 
> 이때 가장 중요한 규칙이 있습니다! 앞서 만든 `FileStorage`가 `save()`와 `load()`라는 버튼을 갖고 있었죠? 새로운 `SQLiteStorage`도 반드시 똑같은 이름의 `save()`, `load()` 버튼을 가져야 합니다. 그래야 나중에 본체에서 어떤 부품을 끼우든 똑같은 버튼을 눌러서 저장할 수 있으니까요. 이런 나사 구멍 규격을 맞추는 게 DI 패턴의 핵심입니다. AI에게 이 규격을 명시해서 코드를 짜달라고 해보죠."

## 🎙️ 실습 가이드 (해당 시)

### 실습 목표
- 이 실습을 통해 수강생은 DB를 활용하는 저장 모듈을 생성하되, 이전 모듈과 인터페이스(메서드명)를 동일하게 맞추는 설계 원칙을 연습한다.

### 단계별 지시
| 단계 | 소요 시간 | 강사 지시사항 | 예상 결과 |
|------|----------|--------------|----------|
| 1 | 5분 | "새 파일 `sqlite_storage.py`를 만들고, AI에게 'sqlite3를 사용해서 고객 데이터를 DB에 저장하는 `SQLiteStorage` 클래스를 만들어줘. 반드시 `save()`와 `load()` 메서드를 가져야 해'라고 요청하세요." | DB 연동 코드가 포함된 클래스 코드 출력 |
| 2 | 5분 | "생성된 코드를 파일에 붙여넣습니다. 코드 안에 `CREATE TABLE` 이나 `INSERT INTO` 같은 SQL 언어가 살짝 보일 거예요." | DB 조작을 위한 내부 로직(SQL) 파악 |
| 3 | 5분 | "마찬가지로 맨 밑에 테스트 코드를 넣어 실행해 봅니다. 이번엔 폴더에 `.db` 파일이 생겼는지 확인하세요." | `customers.db` 파일이 생성되어 정상 동작 확인 |

### 주의사항 및 트러블슈팅
- 흔한 오류 1: 수강생이 SQL 문법을 모두 이해하려고 부담을 느끼는 경우 (해결법: 지금은 쿼리문(SQL) 자체보다 '클래스가 DB 조작을 캡슐화해준다'는 아키텍처적 관점에만 집중하라고 안심시킵니다.)

## 💡 비유 및 스토리텔링

### 핵심 비유
- **비유**: 종이 장부(파일)와 전산 시스템(DB)
- **설명**: 둘 다 데이터를 저장하는 목적은 같지만, 규모가 커질수록 전산 시스템(DB)이 검색과 저장 속도에서 압도적입니다.
- **AI 시대의 서사 연결**: "예전 개발자들은 이 복잡한 SQL 쿼리문을 토씨 하나 안 틀리고 외워야 했어요. 하지만 이제는 '고객 테이블 만들고 저장하는 코드 짜줘' 하면 AI가 완벽한 SQL을 뱉어냅니다. 여러분은 '어떤 저장소가 필요한가?'(설계)만 고민하면 됩니다."

### 스토리텔링 포인트
- 감정적 연결: 복잡해 보이는 데이터베이스 프로그래밍조차 AI의 도움과 명확한 지시(프롬프트)를 통해 순식간에 구현해 내는 성취감을 극대화합니다.
- 전환 멘트: "드디어 2종류의 타이어 세트가 모두 완성되었습니다! 이제 자동차 본체(CustomerManager)에 이 타이어들을 번갈아 끼워가며 신나게 달릴 준비 되셨나요? 마지막 실습으로 가시죠!"

## 🔗 의존성 및 연결성

### 필수 선행 지식
- 세션-098: 파일 저장 모듈(FileStorage) 구현 (동일한 인터페이스 규격의 필요성)

### 권장 선행 지식
- 없음

### 후속 연결
- 세션-100: DI 적용 실습 (v4 저장소 교체 테스트)

### 브릿지 노트 예시
> "타이어 두 개(File, DB)를 든든하게 깎았습니다. 이제 대망의 결승전입니다. 세션 100에서는 메인 프로그램인 `customer_manager_v4.py`를 열어서, 이 두 타이어를 외부에서 주입(DI)하여 저장 방식을 1초 만에 갈아 끼우는 마술을 보여드리겠습니다."

## 📝 Gemini 프롬프트 최적화 태그
```yaml
gemini_optimized:
  chunk_type: code
  complexity: medium
  estimated_chars: 4000
  output_style: continuous_prose
  tone: friendly_spoken_korean
  include:
    - instructor_script: true
    - lab_guide: true
    - analogy: detailed_situation
    - mermaid_diagram: false
  prerequisites:
    mandatory: [98]
    recommended: []
```


## 📚 참고자료 매핑

| 참고자료 | 관련 섹션 | 활용 방법 |
|----------|----------|----------|
| 8 코딩.pdf | §8.14 데이터 영속화 (파일/DB) | JSON/SQLite 저장 모듈 구현 |

## ✅ 체크리스트 (작성자 자가 검증)
- [x] 단 1개의 핵심 학습 목표만 명시했는가?
- [x] 소요 시간이 15~25분 범위인가?
- [x] 강사 대본 예상 분량이 1,500~2,000자 범위인가?
- [x] chunk_type, complexity 태그를 부여했는가?
- [x] 선행/후행 세션을 명확히 연결했는가?
- [x] 비유가 'AI 시대의 서사' 톤과 일치하는가?
