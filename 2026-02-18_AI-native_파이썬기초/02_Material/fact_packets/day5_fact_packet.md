# Day 5 팩트 패킷 (세션 086-106)
> 3-Source Mandatory | Source A: 로컬 참고자료 | Source B: NotebookLM | Source C: Deep Research

## 세션별 교육 콘텐츠

### 세션 086: 클래스와 인스턴스의 개념
- **📚 참고자료 매핑**: `8 코딩.pdf` (OOP 기초) [Source A], `day5_notebooklm.md` (클래스와 인스턴스) [Source B], `day5_deep_research.md` (OOP for Absolute Beginners) [Source C]
- **정의 및 핵심 개념**: 클래스는 객체를 만들기 위한 '설계 도면' 또는 템플릿이며, 인스턴스는 이 도면을 바탕으로 만들어진 '실제 생산품(객체)'. 붕어빵 틀(클래스)과 붕어빵(인스턴스) 비유를 사용하며, 각 인스턴스는 독립적인 데이터 공간을 가짐. 절차적 프로그래밍의 한계를 극복하고 데이터와 기능을 하나로 묶는 패러다임 전환.
- **실습 예시**:
```python
class Student:
    pass

s1 = Student() # 붕어빵 1
s2 = Student() # 붕어빵 2
```
- **강사 스크립트 포인트**: "앞서 함수와 딕셔너리로 데이터를 관리할 때 변수들이 흩어져 불편하셨죠? 이제 붕어빵 틀처럼 데이터와 행동을 하나로 묶어내는 마법, 클래스를 만나볼 시간입니다."

### 세션 087: 속성과 메서드, 그리고 `__init__`
- **📚 참고자료 매핑**: `8 코딩.pdf` (클래스 구조) [Source A], `day5_notebooklm.md` (속성과 메서드) [Source B], `day5_deep_research.md` (클래스와 인스턴스) [Source C]
- **정의 및 핵심 개념**: 속성(Attribute)은 객체의 데이터(상태)이고, 메서드(Method)는 객체의 기능(동작). `__init__`은 인스턴스 생성 시 자동 호출되는 초기화 메서드이며, `self`는 생성된 인스턴스 자신을 가리킴.
- **실습 예시**:
```python
class Customer:
    def __init__(self, name, phone):
        self.name = name    # 속성
        self.phone = phone  # 속성
    
    def display(self):      # 메서드
        print(f"{self.name}: {self.phone}")
```
- **강사 스크립트 포인트**: "`self`는 '나 자신'을 뜻합니다. `self.name`은 '내 이름'이라는 뜻이죠. 초기화 메서드 `__init__`은 붕어빵 틀에 반죽을 붓는 첫 과정과 같습니다."

### 세션 088: 나만의 클래스 만들기 실습
- **📚 참고자료 매핑**: `8 코딩.pdf` (OOP 기초 실습) [Source A], `day5_notebooklm.md` (클래스와 인스턴스) [Source B]
- **정의 및 핵심 개념**: 배운 클래스 기본 문법을 활용해 현실의 객체를 모델링하는 실습. 속성과 메서드를 정의하여 독립된 인스턴스들을 생성하고 동작을 확인.
- **실습 예시**:
```python
c1 = Customer("홍길동", "010-1234-5678")
c2 = Customer("김철수", "010-9876-5432")
c1.display()
c2.display()
```
- **강사 스크립트 포인트**: "여러분 주변에 있는 사물 하나를 선택해 클래스로 만들어보세요. 상태는 속성으로, 동작은 메서드로 표현하면 됩니다."

### 세션 089: 데이터를 담는 특별한 그릇, `@dataclass`
- **📚 참고자료 매핑**: `8 코딩.pdf` (`@dataclass` 활용) [Source A], `day5_notebooklm.md` (`@dataclass` 데코레이터) [Source B], `day5_deep_research.md` (Python `@dataclass`) [Source C]
- **정의 및 핵심 개념**: Python 3.7+부터 지원되며, 데이터 저장을 위한 클래스 생성을 효율적으로 돕는 데코레이터. `__init__`, `__repr__`, `__eq__` 등 보일러플레이트 코드를 자동 생성.
- **실습 예시**:
```python
from dataclasses import dataclass, field

@dataclass
class Customer:
    name: str
    email: str
    age: int
    points: int = 0
```
- **강사 스크립트 포인트**: "매번 `def __init__(self...): self.name = name` 치기 번거로우셨죠? `@dataclass`를 쓰면 이 모든 반복 작업이 사라집니다."

### 세션 090: 객체지향 리팩토링과 캡슐화 개념
- **📚 참고자료 매핑**: `8 코딩.pdf` (캡슐화와 접근 제어) [Source A], `day5_notebooklm.md` (객체지향 리팩토링과 캡슐화) [Source B], `day5_deep_research.md` (캡슐화와 OOP 리팩토링) [Source C]
- **정의 및 핵심 개념**: 내부 구조를 숨기고 외부에는 필요한 인터페이스만 공개. `__` (더블 언더스코어)로 비공개 속성 구현. 리팩토링은 코드의 동작은 유지한 채 내부 구조를 개선하는 작업.
- **실습 예시**:
```python
class BankAccount:
    def __init__(self, balance):
        self.__balance = balance  # 비공개 속성
```
- **강사 스크립트 포인트**: "자동차 운전할 때 엔진 원리를 몰라도 페달만 밟으면 되죠? 캡슐화는 복잡한 내부를 숨겨서 객체를 안전하고 쉽게 쓰게 만드는 기술입니다."

### 세션 091: 고객관리 v3로의 진화 (OOP 적용)
- **📚 참고자료 매핑**: `8 코딩.pdf` (리팩토링) [Source A], `day5_deep_research.md` (고객관리 v2→v3 진화) [Source C]
- **정의 및 핵심 개념**: 함수 중심의 구조(v2)에서 클래스 중심(v3)으로 패러다임 전환. `Customer` 데이터 클래스와 이를 관리하는 `CustomerManager` 클래스로 분리.
- **실습 예시**:
```python
class CustomerManager:
    def __init__(self):
        self._customers: list[Customer] = []
        
    def add(self, customer: Customer):
        self._customers.append(customer)
```
- **강사 스크립트 포인트**: "이제 리스트와 딕셔너리로 관리하던 고객 데이터를 `Customer` 객체로 격상시킬 때입니다. 코드가 어떻게 역할 단위로 정리되는지 느껴보세요."

### 세션 092: 데이터 검증 함수 추가
- **📚 참고자료 매핑**: `day5_notebooklm.md` (캡슐화) [Source B], `day5_deep_research.md` (`@property` — Pythonic한 캡슐화) [Source C]
- **정의 및 핵심 개념**: Python의 `@property` 데코레이터를 이용해 비공개 변수에 접근하고, setter를 통해 안전한 데이터 검증 로직 추가.
- **실습 예시**:
```python
    @property
    def age(self) -> int:
        return self._age

    @age.setter
    def age(self, value: int) -> None:
        if value < 0:
            raise ValueError("나이는 0 이상이어야 합니다.")
        self._age = value
```
- **강사 스크립트 포인트**: "나이에 -10살이 들어오면 안되겠죠? `@property`와 setter를 쓰면 밖에서는 변수처럼 쓰면서 안에서는 강력한 검증문을 통과시킬 수 있습니다."

### 세션 093: 캡슐화 실습 및 v3 코드 리뷰
- **📚 참고자료 매핑**: `8 코딩.pdf` (코드 리뷰) [Source A], `9 디 버깅, 테스트, 배포.pdf` (코드 리뷰 프로세스) [Source A], `day5_deep_research.md` [Source C]
- **정의 및 핵심 개념**: v3로 리팩토링된 코드를 보며 캡슐화가 잘 지켜졌는지 점검. 코드 리뷰 5대 체크포인트 활용.
- **강사 스크립트 포인트**: "AI가 짠 코드라도 캡슐화 원칙이 위배되었다면 반려해야 합니다. '이 속성이 밖에서 직접 변경 가능한가?'를 항상 묻는 리뷰어가 되어보세요."

### 세션 094: 상속의 개념과 필요성
- **📚 참고자료 매핑**: `8 코딩.pdf` (상속과 코드 재사용) [Source A], `day5_notebooklm.md` (상속과 super) [Source B], `day5_deep_research.md` (상속) [Source C]
- **정의 및 핵심 개념**: 부모 클래스의 특성과 기능을 자식 클래스가 물려받아 재사용 및 확장. 코드 중복을 제거하고 계층 구조를 만듦.
- **강사 스크립트 포인트**: "일반 고객 말고 VIP 고객을 만들어야 한다면? 똑같은 코드를 다시 복붙할 필요 없이 상속으로 재산(기능)을 물려받으면 됩니다."

### 세션 095: 기존 클래스 확장하기 (VIPCustomer와 super)
- **📚 참고자료 매핑**: `8 코딩.pdf` (상속) [Source A], `day5_notebooklm.md` (상속) [Source B], `day5_deep_research.md` (상속) [Source C]
- **정의 및 핵심 개념**: `super()` 함수를 사용해 자식 클래스에서 부모 클래스의 `__init__` 등 메서드를 호출, 로직 확장.
- **실습 예시**:
```python
class VIPCustomer(Customer):
    def __init__(self, name, email, age, discount_rate):
        super().__init__(name, email, age)  # 부모 초기화 호출
        self.discount_rate = discount_rate
```
- **강사 스크립트 포인트**: "`super()`를 부르면 부모가 해놓은 초기 셋팅을 그대로 가져올 수 있습니다. 우린 VIP만의 특별한 할인율만 신경 쓰면 되죠."

### 세션 096: 다형성 체험 (리모컨의 전원 버튼)
- **📚 참고자료 매핑**: `8 코딩.pdf` (다형성) [Source A], `day5_notebooklm.md` (다형성) [Source B], `day5_deep_research.md` (다형성) [Source C]
- **정의 및 핵심 개념**: "같은 명령, 다른 결과". 메서드 오버라이딩을 통해 부모의 메서드를 자식에게 맞게 재정의. Duck Typing의 개념 적용.
- **실습 예시**:
```python
def process_discount(customer):
    # 일반 고객과 VIP 고객이 같은 메서드를 다르게 반응
    print(customer.get_discount()) 
```
- **강사 스크립트 포인트**: "리모컨 전원 버튼 하나로 TV도 켜고 에어컨도 켜듯이, 똑같은 `get_discount()` 함수를 불렀지만 고객 타입에 따라 스스로 다른 할인율을 계산해냅니다."

### 세션 097: 의존성 주입(DI) 개념
- **📚 참고자료 매핑**: `8 코딩.pdf` (의존성 주입 패턴) [Source A], `day5_notebooklm.md` (의존성 주입) [Source B], `day5_deep_research.md` (의존성 주입) [Source C]
- **정의 및 핵심 개념**: 내부에서 사용할 의존성(저장소)을 직접 생성하지 않고 외부 생성자를 통해 주입받는 패턴. 결합도를 낮추고 테스트 유연성 확보.
- **강사 스크립트 포인트**: "바리스타가 직접 원두를 기르는 대신 원두를 납품받아 커피를 내리는 것과 같습니다. 파일에 저장하든 DB에 저장하든 Manager(바리스타)는 알 필요가 없죠."

### 세션 098: 파일 저장 모듈(FileStorage) 구현
- **📚 참고자료 매핑**: `8 코딩.pdf` (데이터 영속화) [Source A], `day5_notebooklm.md` (파일/DB 저장 모듈) [Source B], `day5_deep_research.md` (JSON 파일 저장) [Source C]
- **정의 및 핵심 개념**: `json` 모듈을 활용하여 객체 데이터를 텍스트 파일로 직렬화(Serialization)하여 영구 저장.
- **실습 예시**:
```python
import json
from dataclasses import asdict

class JsonFileStorage(Storage):
    def save(self, customers):
        data = [asdict(c) for c in customers]
        with open("customers.json", "w") as f:
            json.dump(data, f)
```
- **강사 스크립트 포인트**: "프로그램이 꺼지면 날아가는 메모리의 한계를 넘어, 물리적인 파일로 고객 데이터를 영구 보존해봅시다."

### 세션 099: DB 저장 모듈(SQLiteStorage) 구현
- **📚 참고자료 매핑**: `8 코딩.pdf` (데이터 영속화) [Source A], `day5_notebooklm.md` (파일/DB 저장) [Source B], `day5_deep_research.md` (SQLite 기초) [Source C]
- **정의 및 핵심 개념**: 내장 데이터베이스인 `sqlite3`를 활용해 대용량 데이터 관리에 적합한 SQL 기반 영구 저장소 구현.
- **실습 예시**:
```python
import sqlite3

class SQLiteStorage(Storage):
    def save(self, customers):
        conn = sqlite3.connect("customers.db")
        # INSERT 로직 구현
        conn.commit()
```
- **강사 스크립트 포인트**: "데이터가 수백만 건이 되면 텍스트 파일로는 감당이 안 됩니다. 파이썬에 내장된 강력한 SQLite 데이터베이스로 저장소를 확장해보죠."

### 세션 100: DI 적용 실습 (v4 저장소 교체 테스트)
- **📚 참고자료 매핑**: `8 코딩.pdf` (DI 패턴) [Source A], `day5_notebooklm.md` (DI 적용 저장소 교체) [Source B], `day5_deep_research.md` (Storage 인터페이스 패턴) [Source C]
- **정의 및 핵심 개념**: 비즈니스 로직 수정 없이 저장 방식을 JSON 파일에서 SQLite DB로 손쉽게 교체.
- **실습 예시**:
```python
# DI 주입
manager1 = CustomerManager(JsonFileStorage())
manager2 = CustomerManager(SQLiteStorage())
```
- **강사 스크립트 포인트**: "놀랍지 않나요? `CustomerManager` 코드는 단 한 줄도 바꾸지 않고, 블록을 갈아 끼우듯 데이터베이스가 바뀌었습니다. 이게 아키텍처의 힘입니다."

### 세션 101: 고객관리 프로그램 진화 과정 총정리
- **📚 참고자료 매핑**: `8 코딩.pdf` (절차→구조 전체) [Source A], `day5_notebooklm.md` (프로그램 진화 총정리) [Source B], `day5_deep_research.md` (고객관리 프로그램 진화 총정리) [Source C]
- **정의 및 핵심 개념**: 5일간의 진화 요약. v1(절차적) → v2(구조적) → v3(OOP) → v4(DI + 아키텍처). 스파게티 코드에서 모듈화를 거쳐 유지보수 가능한 객체지향/DI 구조로의 성장.
- **강사 스크립트 포인트**: "단순한 while문 한 덩어리(v1)였던 우리 코드가 함수(v2)로, 객체(v3)로, 그리고 확장 가능한 아키텍처(v4)로 진화했습니다. 패러다임의 변화를 몸소 체험하신 겁니다."

### 세션 102: "AI 시대의 서사" 최종 연결 및 SDD 회고
- **📚 참고자료 매핑**: `AI 시대의 서사 v3 - Claude.md` [Source A], `day5_notebooklm.md` (AI 시대의 서사 최종 연결) [Source B], `day5_deep_research.md` [Source C]
- **정의 및 핵심 개념**: "코드를 타이핑하는 사람에서 아키텍트로". 코딩보다 명세(Specification)와 문제 정의(SDD)가 중요한 이유 재확인. AI는 훌륭한 네비게이션/통역사이나 목적지는 결국 인간이 정해야 함.
- **강사 스크립트 포인트**: "여러분이 5일간 스펙을 짜고 리뷰했던 이유는 하나입니다. 코드를 빨리 치는 사람이 아니라, '무엇을 만들지' AI에게 명확히 지시하고 검증하는 아키텍트가 되기 위해서입니다."

### 세션 103: 수료 프로젝트 코드 리뷰 공유
- **📚 참고자료 매핑**: `8 코딩.pdf` (코드 리뷰) [Source A], `9 디 버깅, 테스트, 배포.pdf` (코드 리뷰 프로세스) [Source A]
- **정의 및 핵심 개념**: 수강생 각자의 최종 고객관리 v4 결과물을 공유하고, 캡슐화와 다형성, DI 적용 여부를 서로 코드 리뷰.
- **강사 스크립트 포인트**: "코딩에는 정답이 없습니다. 옆 팀은 저장소를 어떻게 주입했는지, 객체를 어떻게 나누었는지 리뷰하며 통찰을 나눠봅시다."

### 세션 104: 파이썬 기초 전 과정 핵심 퀴즈
- **📚 참고자료 매핑**: `8 코딩.pdf` (파이썬 기초 전체) [Source A]
- **정의 및 핵심 개념**: Day 1 ~ Day 5 과정(변수, 제어문, 자료구조, 함수, OOP, DI)을 리뷰하는 퀴즈를 통한 리마인드.
- **강사 스크립트 포인트**: "기억을 되살리는 가장 좋은 방법은 인출입니다. 5일간의 여정을 가벼운 퀴즈로 짚어볼까요?"

### 세션 105: 수료 후 학습 로드맵 및 심화 학습 상담
- **📚 참고자료 매핑**: `day5_deep_research.md` (심화 학습 로드맵) [Source C]
- **정의 및 핵심 개념**: 과정 수료 이후 웹 개발(FastAPI), 데이터 분석(pandas), 자동화 도구, AI/ML 통합 등 학습 방향 제시.
- **강사 스크립트 포인트**: "기초는 완성되었습니다. 이제 파이썬이라는 도구로 웹 서버를 만들지, 데이터를 분석할지, 나만의 AI 비서를 만들지 선택할 시간입니다."

### 세션 106: 최종 마무리 및 수료식
- **📚 참고자료 매핑**: `AI 시대의 서사 v3 - Claude.md` [Source A]
- **정의 및 핵심 개념**: 전체 교육에 대한 회고, 학습 성취도 축하, 수료증 수여 및 소감 발표.
- **강사 스크립트 포인트**: "비전공자에서 시작해 5일 만에 객체지향 아키텍처까지 도달하신 여러분을 환영합니다. 이제 여러분은 당당한 AI-native 개발자입니다. 수고 많으셨습니다!"