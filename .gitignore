# ============================================================
# Lecture Factory .gitignore
# ============================================================

# ── macOS ────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# ── 시크릿 / 환경변수 (API 키 포함) ─────────────────────────
.env
.env.*
.env.local
.env.*.local
!.env.template

# ── Python ───────────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/
*.egg

# ── Python 도구 캐시 ─────────────────────────────────────────
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# ── Node.js ──────────────────────────────────────────────────
node_modules/
package-lock.json
yarn.lock
bun.lock
.npm/
.yarn/
package.json
bun.lock

# ── AI 에이전트 런타임 / IDE 상태 ────────────────────────────
.omc/
.opencode/*
!.opencode/oh-my-opencode.jsonc
!.opencode/.gitignore
!.opencode/package.json

# ── Gemini CLI 스킬 런타임 데이터 (브라우저 상태, 인증) ──────
.agent/skills/notebooklm/data/

# ── 생성 산출물: PPTX / 이미지 / 중간 HTML ──────────────────
# 마크다운 소스에서 재생성 가능한 바이너리 산출물
*.pptx
*/05_PPTX/**/html/
*/06_NanoPPTX/**/images/
Specification/

# ── 참고자료 바이너리 (PDF, ZIP 등) ──────────────────────────
*.pdf
*.zip
*.tar.gz
*.rar
*.7z

# ── 참고자료 / 기획 메모 ─────────────────────────────────────
참고자료/
AI-native_자바기초.md
AI-native_스프링부트기초.md

# ── 강의 프로젝트 산출물 폴더 (날짜 prefix) ──────────────────
# 파이프라인 실행 결과물 — 재생성 가능
20[0-9][0-9]-*/

# ── 기타 바이너리 / 대용량 ───────────────────────────────────
*.mp4
*.mp3
*.wav
*.avi
*.mov

# ── 로그 / 임시 파일 ────────────────────────────────────────
*.log
*.jsonl
.agent/dashboard/*.md
*.tmp
*.swp
*.swo
*~
*.bak

# ── IDE / 에디터 ─────────────────────────────────────────────
.idea/
.vscode/
*.code-workspace
