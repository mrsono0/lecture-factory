{
  "$schema": "https://raw.githubusercontent.com/code-yeongyu/oh-my-opencode/master/assets/oh-my-opencode.schema.json",
  // ============================================================
  // Lecture Factory - Model Routing Configuration
  // ============================================================
  // Single Source of Truth for all model routing decisions.
  // Categories define MODEL CAPABILITY TIERS, not agent aliases.
  //
  // Resolution flow:
  //   1. A0 Orchestrator reads .agent/AGENTS.md §Per-Agent Model Routing
  //   2. Looks up agent → category mapping
  //   3. This file resolves category → model
  //
  // Domain context (prompt_append) lives in each agent's .md file,
  // NOT in categories. Categories are pure model routing only.
  // ============================================================

  // === Agent-Level Model Overrides ===
  "agents": {
    // Main Orchestrator: Multi-pipeline coordination (8 pipelines)
    // Kimi K2.5 — Agent Swarm (100 sub-agents), tool-use #1, 256K context, ~30x cost reduction
    "sisyphus": {
      "model": "opencode/kimi-k2.5"
    },
    // Codebase Explorer: Pattern search within lecture materials
    // Fast & cheap - just grep/read operations
    "explore": {
      "model": "anthropic/claude-sonnet-4-6"
    },
    // Reference Researcher: Technical docs, library lookups
    // Anthropic Sonnet 4.6 — superior document parsing & structured output quality
    "librarian": {
      "model": "anthropic/claude-sonnet-4-6"
    },
    // Architecture Consultant: Curriculum/material structure review
    // Reasoning-specialized model for strategic decisions
    "oracle": {
      "model": "opencode/gpt-5.2",
      "variant": "high"
    },
    // Deep Executor: Complex autonomous goal-oriented tasks
    // Reasoning-specialized for multi-step implementation
    "hephaestus": {
      "model": "opencode/gpt-5.3-codex"
    }
  },

  // === Category-Level Model Routing (9 Consolidated Categories) ===
  // Used via task(category="...", prompt="...")
  // Consolidated from 19 → 9 categories by merging same-model tiers.
  // See .agent/plans/model-routing-consolidation-plan.md for mapping details.
  "categories": {
    // --- Tier 1: Orchestration (pipeline coordination, quality gates) ---
    // Absorbs: unspecified-low
    "orchestration": {
      "model": "opencode/claude-sonnet-4-6"
    },

    // --- Tier 2: Deep Writing (long-form lecture scripts, highest quality) ---
    // Absorbs: deep, micro-writing, unspecified-high
    "deep-writing": {
      "model": "anthropic/claude-opus-4-6",
      "variant": "max"
    },

    // --- Tier 3: Fast Task (mechanical work — validation, conversion, execution) ---
    // Absorbs: quick
    "fast-task": {
      "model": "anthropic/claude-haiku-4-5"
    },

    // --- Tier 4: Visual & Creative (slides, research, artistry, 1M context) ---
    // Absorbs: visual-engineering, artistry, research, writing, curriculum-chunking
    "visual-creative": {
      "model": "google/antigravity-gemini-3.1-pro",
      "variant": "high"
    },

    // --- Tier 5: Fast Extraction (structured extraction, tracing, 1M context) ---
    // Absorbs: gemini-flash
    "fast-extraction": {
      "model": "google/antigravity-gemini-3-flash"
    },

    // --- Tier 6: Structural (curriculum design, source mining, aggregation) ---
    // Absorbs: glm5, curriculum-architecture, material-aggregation
    "structural": {
      "model": "opencode/glm-5"
    },

    // --- Tier 7: Quality Gate (QA defense line, maximum reasoning) ---
    // Absorbs: ultrabrain, reasoning-high, reasoning-xhigh
    "quality-gate": {
      "model": "openai/gpt-5.3-codex",
      "variant": "xhigh"
    },

    // --- Tier 8: Codex Support (instructor support content generation) ---
    // Absorbs: instructor-support-codex
    "codex-support": {
      "model": "openai/gpt-5.3-codex"
    },

    // --- Tier 9: Korean Editing (copy & tone editing for Korean text) ---
    // Absorbs: antigravity-sonnet
    "korean-editing": {
      "model": "google/antigravity-claude-sonnet-4-6"
    }
  }
}
